# 献立提案アプリ Phase 2 仕様書（献立カレンダー・星評価）

作成日: 2026-07-11 / 想定実装: Claude Code（**Claude Sonnet**）

## 0. 前提

- **Phase 1の完成・受け入れ後に着手する**（Phase 1の実機確認結果によって本書は微修正の可能性あり）
- Phase 1仕様書と**必ずセットで**Sonnetに渡すこと。Phase 1 §11.2の禁止事項・進め方は本Phaseでも引き続き適用
- Phase 1で作った単一HTMLファイルへの追加実装（ファイルは1つのまま）

## 1. 目的

- 採用した献立に「いつ作るか」を割り当て、カレンダーで見返せるようにする
- 作った料理を星評価＋一言メモで記録し、Phase 3「好みを学習する提案」の元データを貯める
- 「今日なに作るんだっけ」をゼロ秒で解決する（ホームに今日の献立を常時表示）

## 2. 機能仕様

### 2.1 採用フローの変更（日付割り当て）

- Phase 1: 採用確定 → 買い物リスト
- Phase 2: 採用確定 → **日付割り当て画面** → 買い物リスト
- 日付割り当て画面の仕様:
  - 採用したメニューを今日から順に自動で仮割り当てして表示
  - 各行の日付をタップして変更可能（外食日などをスキップできる）
  - 同日に複数は割り当て不可（バリデーション）
  - 「この並びで確定」ボタンで保存し買い物リストへ

### 2.2 献立カレンダー画面（新規）

- 月表示カレンダー。各日にメニュー名を短縮表示、タップで日付詳細へ
- 日付詳細で表示・操作できること:
  - レシピ全文（説明・材料・手順・kid_note）
  - **星評価（1〜5）＋一言メモ**の入力
  - 別の日へ移動 / 割り当て解除
- 前月・翌月への移動が可能（過去の振り返り用）

### 2.3 ホーム画面への追加

- 最上部に「今日の献立」カードを表示（メニュー名・調理時間、タップでレシピ詳細）
- 今日の割り当てがない日は非表示（空の枠を出さない）
- **未評価の催促**: 昨日以前の未評価メニューが1件以上ある場合のみ、控えめなカードを1枚だけ表示（「昨日の〇〇、どうでしたか？」→ タップで評価入力）。スキップ可能で、しつこく再表示しない（1日1回まで）。労いトーン原則（Phase 1 §7）を適用

### 2.4 星評価

- 対象: adoptedHistory の各メニュー
- 星1〜5 ＋ 任意の一言メモ（プレースホルダ例:「子どもがよく食べた」「ちょっと辛すぎた」）
- メモはPhase 3でそのままプロンプト素材になるため、テキストで保存
- 評価は後から変更可能

### 2.5 料理ライブラリ画面（新規）

- これまで作った料理の一覧。並び順: 星の高い順（未評価は末尾）/ 新しい順 の切り替え
- 各行: 料理名・星・最後に作った日。タップでレシピ詳細
- **「もう一度作る」ボタン**: AIを介さずその料理を再採用 → 日付割り当て → 買い物リストに材料が反映される（リピート運用の中核機能）

## 3. データ設計の変更

adoptedHistory の各要素を拡張:

```json
{
  "adoptedAt": "2026-07-11",
  "plannedDate": "2026-07-13",
  "rating": null,
  "ratingMemo": "",
  "menu": { Phase 1のメニューJSON }
}
```

- **マイグレーション必須**: Phase 1で保存済みの既存データは plannedDate 等を持たない。初回読み込み時に補完する（plannedDate ← adoptedAt、rating ← null）。既存データを壊さないこと
- 変更はすべて StorageService 経由（Phase 1 §6の原則を維持）

## 4. 画面構成の変更

- 下部タブを5つに: **ホーム / カレンダー / ライブラリ / 買い物リスト / 設定**
- 買い物リストは「最後に生成したリスト」を保持して表示（タブから再訪できる）
- 提案結果・日付割り当ては遷移画面（タブではない）

## 5. 実装ステップ（Sonnet向け・この順で1ステップずつ）

| Step | 内容 | 動作確認方法 |
|---|---|---|
| 1 | データ構造拡張＋マイグレーション | Phase 1の既存データが壊れず読み込める |
| 2 | 日付割り当て画面（採用フロー変更） | 採用→日付調整→買い物リストが通る |
| 3 | カレンダー画面（月表示・日付詳細・移動/解除） | 割り当てが正しく表示・移動できる |
| 4 | ホームの「今日の献立」カード | 当日分だけが表示される |
| 5 | 星評価＋メモ（日付詳細・未評価催促カード） | 評価がリロード後も保持される |
| 6 | 料理ライブラリ（一覧・ソート・もう一度作る） | 再採用が日付割り当て→買い物リストまで通る |
| 7 | 仕上げ＋受け入れ条件チェック | §7の全項目合格 |

## 6. 制約（Phase 1 §11.2に追加）

- **既存機能を壊さない**: 各ステップ完了時に、Phase 1受け入れ条件のうち「提案フロー一連」「買い物リストの合算」「リロード後のデータ保持」の3点を回帰確認する
- Googleカレンダー等の外部カレンダー連携はしない（スコープ外のまま）
- 評価の提案への反映はまだ実装しない（Phase 3）。本Phaseは記録まで

## 7. 受け入れ条件（Phase 2完了の定義）

1. 採用確定時に日付を割り当てられ、カレンダーに表示される
2. カレンダーから日付の変更・割り当て解除ができる
3. ホームに今日の献立が表示される（ない日は表示されない）
4. 星評価と一言メモが保存され、リロード後も保持される
5. ライブラリの「もう一度作る」から買い物リストへの反映まで通る
6. Phase 1で保存した既存データが移行後も正常に表示・動作する
7. Phase 1の受け入れ条件がすべて引き続き合格している（回帰）

## 8. Phase 3への引き継ぎメモ

- rating（星）と ratingMemo（一言）が、Phase 3の提案反映の入力データになる
  - 高評価（★4以上）→「この家庭の好みの傾向」としてプロンプトに要約して渡す
  - 低評価（★2以下）→ 重複回避リストと同様の「回避リスト」に加える
  - ratingMemo（「子どもがよく食べた」等）はそのままプロンプト素材に使える
- Phase 2の運用で評価データが2〜3週間分たまってからPhase 3に着手するのが望ましい
