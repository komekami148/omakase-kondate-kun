# 献立提案アプリ（名称未定）Phase 1 仕様書

作成日: 2026-07-11 / 想定実装: Claude Code（**Claude Sonnet**）。実装時は§11の実装ガイドに必ず従うこと。

## 0. この文書について

Claude Codeでの実装を前提としたPhase 1の仕様書。成果物は**単一HTMLファイル1つ**（PWA対応、スマホ縦画面最適化）。

## 1. 背景と目的

- これまでGeminiチャットで毎週の献立相談をしていたが、家族構成・食材バランス・重複回避・こだわり条件など定型部分を毎回手入力していた
- この定型部分をアプリが保持・自動化し、「条件選択 → AI提案 → 採用 → 買い物リスト → LINE共有」を一連の操作で完結させる
- 利用者: 本人（操作）＋家族（買い物リスト・献立をLINEで受け取る）
- 家族構成: 大人2人・子ども2人（3歳・1歳）。提案は常に「子どもが食べやすい」「取り分け対応」を考慮する

## 2. Phase構成（全体ロードマップ）

| Phase | 内容 |
|---|---|
| **1（本書）** | 条件選択 → AI提案 → 採用 → 買い物リスト → LINE共有 |
| 2 | 献立カレンダー表示、星評価の記録 |
| 3 | 評価・履歴の提案反映強化、買い忘れレスキューモード、調理中相談チャット（「辛すぎた」等の失敗レスキュー含む） |
| 4（必要なら） | Firebase(Firestore)による家族リアルタイム共有 |

## 3. 技術方針

- 単一HTMLファイル（HTML/CSS/JSすべて内包。外部ライブラリは原則なし、必要ならCDN最小限）
- スマホ縦画面ファースト。ホーム画面追加で全画面起動できるようPWA簡易対応（manifest内包）
- データ保存: **localStorage**。ただし読み書きは必ず `StorageService` モジュール経由とし、将来Firestoreへ差し替え可能な構造にする（Phase 4対応）
- AI: **Gemini API**（generativelanguage.googleapis.com）。モデルは実装時点の最新軽量モデル（gemini-2.5-flash系など）を公式ドキュメントで確認して採用すること
- APIキーはユーザーが設定画面で入力し localStorage に保存。**コードへの埋め込み禁止**
- 構造化出力: `generationConfig.responseMimeType = "application/json"` を使用（可能なら responseSchema も指定）

## 4. 画面仕様

### 4.1 ホーム（条件選択）画面

1. **回数選択**: 1〜7回（デフォルト5）
2. **メインタンパク質の割り当て**: タンパク質マスタから選び、各項目に +/− で回数を指定。合計＝回数選択と一致するようバリデーション
   - マスタ初期値: 鶏むね / 鶏もも / 豚こま / 豚バラ / ひき肉（合いびき）/ ひき肉（鶏）/ たまご / 豆腐 / 白身魚 / 鮭 / 牛こま
   - ※「豚肉」でなく**部位レベル**で持つ（豚こまとひき肉は別物として扱う）
3. **こだわりタグ**（複数選択チェックボックス）
   - 初期値: 時短（15分以内）/ フライパンひとつ / レンジ調理OK / 旬の野菜 / 子どもが食べやすい / お腹に優しい / 一品で完結 / 麺類以外 / さっぱり / がっつり
4. **自由記入欄**（任意）: プレースホルダ例「ナスとオクラを使い切りたい／木曜は外食」
5. **「献立を考えてもらう」ボタン** → ローディング中は労いメッセージをランダム表示（例:「今週もおつかれさまです。考えますね…」）

前回の選択条件をデフォルト値として復元する（lastConditions）。

### 4.2 提案結果画面

- 提案数 = 指定回数 **+2品**（選択の余地を持たせる）
- カード形式で表示: 料理名 / ひとこと説明 / 使用タンパク質 / 調理時間 / **子ども対応メモ**（取り分け・辛さ調整など）/ 材料（分量つき・タップで展開）/ 手順（3〜6ステップ・タップで展開）
- 各カードに:
  - **「採用」トグル**
  - **「これは変えて」ボタン**: そのカードだけ同条件で再生成。任意で一言理由を入力でき、プロンプトに反映（例:「豚こまのイメージだった」→ 修正提案）
- 画面下部: 「全部作り直す」/「採用を確定（n件）」
- 採用確定 → adoptedHistory に保存し、買い物リスト画面へ遷移

### 4.3 買い物リスト画面

- 採用レシピの材料を**統合表示**
  - 同一食材は合算（例: 玉ねぎ1個 + 1/2個 → 1.5個）。単位が異なり合算できない場合は併記
  - **常備品リスト**（設定で編集可）に含まれる食材は「常備品（在庫確認）」セクションへ分離
  - 並び順: 野菜 → 肉・魚 → その他 → 常備品確認
- 各行にチェックボックス（買い物中の消し込み用）
- **「LINEに送る」ボタン**: Web Share API（navigator.share）でテキスト共有。非対応環境では「コピー」ボタンにフォールバック
- 共有テキストフォーマット例:

```
【今週の買い物リスト】
▼野菜
・玉ねぎ 1.5個
・ナス 3本
▼肉・魚
・豚こま 300g
▼常備品の確認
・カレー粉、片栗粉

【今週の献立】
・鶏むねのさっぱりポン酢炒め
・丸めない酢豚風
...
```

### 4.4 設定画面

- Gemini APIキー（マスク表示＋接続テストボタン）
- 家族プロフィール（自由記述。初期値:「大人2人、3歳と1歳の子ども。子どもが食べやすい味付け・取り分けを重視」）
- タンパク質マスタの追加・削除・並べ替え
- こだわりタグの追加・削除
- 常備品リストの編集（初期値: 醤油/味噌/みりん/酒/砂糖/塩/こしょう/サラダ油/ごま油/ケチャップ/マヨネーズ/カレー粉/片栗粉/小麦粉/だしの素）
- データのエクスポート／インポート（JSON。機種変更・バックアップ用）

## 5. AI連携仕様

### 5.1 プロンプト構成（毎回アプリが自動生成）

固定部（システム相当）:
- 役割: 家庭料理の献立アドバイザー。応答冒頭に一言の労い・共感を添える（絵文字は控えめ）
- 家族プロフィール（設定から挿入）
- 出力はJSONのみ、指定スキーマ厳守

可変部（ユーザー入力から挿入）:
- 提案数（回数+2）、タンパク質割り当て、選択タグ、自由記入欄の内容
- **直近14日の採用メニュー名一覧** →「これらと同じ・似た料理は避けること」（重複回避の自動化）
- 現在の月（旬の野菜の判断材料として）

### 5.2 出力JSONスキーマ

```json
{
  "greeting": "労いのひとこと（1文）",
  "menus": [
    {
      "name": "料理名",
      "description": "ひとこと説明",
      "protein": "使用タンパク質（マスタの名称と一致させる）",
      "cooking_time_min": 15,
      "kid_note": "子ども向けの取り分け・調整メモ",
      "ingredients": [
        { "name": "玉ねぎ", "amount": 1, "unit": "個", "category": "野菜" }
      ],
      "steps": ["手順1", "手順2"]
    }
  ]
}
```

- category は `野菜 | 肉魚 | その他` の3値
- amount は数値、単位は unit に分離（買い物リストの合算処理のため）

### 5.3 エラー処理

- JSONパース失敗 → 自動リトライ1回 → それでも失敗ならエラー表示＋「もう一度」ボタン
- APIキー未設定 → 設定画面へ誘導するメッセージ
- 通信エラー → トースト表示（責めないトーンで）

## 6. データ設計（すべてStorageService経由）

| キー | 内容 |
|---|---|
| `settings` | { apiKey, familyProfile, proteinMaster[], tagMaster[], pantryStaples[] } |
| `adoptedHistory` | [ { adoptedAt(ISO日付), menu(スキーマ1件分), rating: null } ] ※ratingはPhase 2で使用 |
| `lastConditions` | 前回の選択条件（次回起動時のデフォルト） |

`StorageService` は get / set / remove を提供する薄いラッパーとし、内部実装（localStorage）を後からFirestoreに差し替えられるようにする。

## 7. UIテキスト方針（トーン＆マナー）

- 夕方の調理者に寄り添う**労い・共感トーン**を全体に適用
  - 例: 「献立づくり、今週もおつかれさまです」「焦らなくて大丈夫です」
- エラー時も責めない文言（例:「うまく受け取れませんでした。もう一度だけ試させてください」）

## 8. Phase 1のスコープ外（やらないこと）

- カレンダー表示、星評価、評価の提案への反映（Phase 2〜3）
- 副菜・汁物の提案（**メイン料理のみ**）
- 調理中の相談チャット、買い忘れレスキュー専用モード（Phase 3。当面は自由記入欄で代用）
- 家族間リアルタイム同期（Phase 4）

## 9. 受け入れ条件（Phase 1完了の定義）

1. スマホブラウザで「条件選択 → 提案 → 採用 → 買い物リスト → LINE共有」が一連の操作で完了する
2. 提案がタンパク質の割り当て・回数指定と一致している
3. 直近14日に採用した料理と同名・類似の提案が出ない
4. 材料の合算・常備品分離が正しく動作する
5. APIキーがコード内に含まれていない
6. リロードしても設定・採用履歴・前回条件が保持される

## 10. 付記：設計原則（やり取りログから抽出した「3つのキモ」との対応）

Phase 2以降の機能追加時も、この3原則を判断基準とする。

| キモ | 本仕様での対応 |
|---|---|
| **共感と労いのテキスト** | §7 UIテキスト方針（ローディング・エラー文言含む）＋ AI応答の greeting フィールド（§5.2） |
| **リカバリー機能（ボタン一つで修正）** | 提案段階: §4.2「これは変えて」ボタン（一言理由つき再生成）。調理中の失敗（「辛すぎた」等）は Phase 3 の相談チャットで対応 |
| **文脈の記憶（前後の献立と被らない）** | §5.1 直近14日の採用メニュー名を毎回プロンプトへ自動挿入 |

## 11. 実装ガイド（Claude Code / Sonnet向け・必読）

### 11.1 実装ステップ

**必ずこの順で、1ステップずつ実装すること。** 各ステップ完了時に「動作確認方法」を発注者に提示し、確認を得てから次に進む。

| Step | 内容 | 動作確認方法 |
|---|---|---|
| 1 | 骨組み: 単一HTMLに4画面の遷移＋StorageServiceの実装 | 画面が切り替わり、テスト値がリロード後も残る |
| 2 | 設定画面: 全項目の編集・保存・復元 | 各設定を変更→リロード→保持されている |
| 3 | 条件選択画面: 回数・タンパク質割り当て・タグ・自由記入・バリデーション | 割り当て合計が回数と不一致だとボタンが無効になる |
| 4 | モックAI: GeminiServiceをまず固定サンプルJSONで実装し、提案結果画面を完成させる（採用トグル・展開・「これは変えて」もモックで動作） | APIキーなしで条件選択→提案→採用まで通る |
| 5 | 買い物リスト: 合算・常備品分離・カテゴリ順・チェック・共有テキスト・navigator.share/コピー | サンプルデータで合算結果（1個+1/2個→1.5個）が正しい |
| 6 | 実API接続: モックを本物のGemini APIに差し替え。プロンプト生成・JSON検証・リトライ・エラー処理 | 実キーで提案が返り、履歴の重複回避が効いている |
| 7 | 仕上げ: 労いトーン文言、PWA対応、lastConditions復元、§9受け入れ条件の全項目チェック | §9の6項目すべて合格 |

### 11.2 実装上の制約（禁止事項）

- フレームワーク・ビルドツールを使わない（React / Vue / Vite / npm 不使用）。素のHTML+CSS+JSのみ
- ファイルは最後まで1つ。分割・リファクタリングでの複数ファイル化をしない
- **この仕様書にない機能を勝手に追加しない。** 改善案がある場合は実装せず、テキストで提案として報告する
- GeminiのモデルIDを記憶から推測しない。公式ドキュメントで現行の軽量モデルを確認し、確認できない場合はモデルIDを設定画面から変更可能にして対処する
- APIキーをコード・コメント・ログに書かない
- コード内コメントは日本語で書く

### 11.3 コード構成（単一ファイル内のモジュール分け）

- `StorageService`: localStorage読み書きの唯一の窓口（§6）
- `GeminiService`: プロンプト生成・API呼び出し・JSON検証。モック/実APIを切り替え可能にし、モックは「デモモード」として設定画面からON/OFFできる形で残す（APIキーなしで家族にデモ可能）
- `ShoppingList`: 材料の合算・分類ロジック。UIから独立した純粋関数群として実装（単体で検証しやすくする）
- `UI`: 画面描画・イベント処理・状態管理
